{
  "name": "@cucumber/json-formatter",
  "private": true,
  "version": "2.1.0",
  "license": "MIT",
  "scripts": {
    "test": "echo 'no tests'",
    "lint": "echo 'nothing to lint'",
    "lint-fix": "echo 'nothing to lint-fix'",
    "build": "echo 'nothing to build'"
  },
  "devDependencies": {
    "@cucumber/fake-cucumber": "file:../../fake-cucumber/javascript"
  },
  "description": "This module is only used for testing the Go implementation. It generates ndjson  protobuf files (`.ndjson`) from test data in `../ruby`, using `../fake-cucumber`. These files are then piped to the Go JSON formatter during acceptance tests.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "@cucumber/messages": "file:../../messages/javascript"
  },
  "keywords": [],
  "author": ""
}
