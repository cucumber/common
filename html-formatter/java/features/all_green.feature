Feature: Statuses
  These steps will all pass in fake-cucumber, because the default status is passed.

  Scenario: Pass-Fail-Skip
    Given three blind mice
    When we see how they run
    Then they should all run after the farmer's wife
